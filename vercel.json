{
  "buildCommand": "pnpm build",
  "installCommand": "pnpm install --frozen-lockfile --allow-build=esbuild",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}